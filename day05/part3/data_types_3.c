#include <stdio.h>

int main(void) {
    char firstChar = 'A';
    char lastChar = 'Z';

    printf("Character: %c, ASCII value: %d\n", firstChar, firstChar);
    printf("Character: %c, ASCII value: %d\n", lastChar, lastChar);

    return 0;
}
